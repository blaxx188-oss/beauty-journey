/**
 * Order Failed Page — Displays when order placement fails.
 * Arabic RTL, responsive, accessible.
 */

import React from "react";
import FailedPageContent from "./FailedPageContent";

export default function FailedPage() {
  return <FailedPageContent />;
}
